<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.MultiViewRecyclerView" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MultiViewRecyclerView" parent="Base.Theme.MultiViewRecyclerView" />

    <style name="Theme.MultiViewRecyclerView.Splash" parent="Theme.SplashScreen">
        <!-- Background color -->
        <item name="windowSplashScreenBackground">@color/white</item>
        <!-- app logo -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <!-- Animation duration -->
        <item name="windowSplashScreenAnimationDuration">500</item>
        <!-- Optional: remove status bar until transition -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowFullscreen">true</item>
        <!-- Optional: Hide Navigation Bar -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- Theme after splash -->
        <item name="postSplashScreenTheme">@style/Theme.MultiViewRecyclerView</item>
    </style>
</resources>